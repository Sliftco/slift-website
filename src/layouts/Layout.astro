---
export interface Props {
	title: string;
}

const { title } = Astro.props as Props;
---

<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width">
  <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
  <link rel="manifest" href="/site.webmanifest">
  <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#7e10d9">
  <meta name="msapplication-TileColor" content="#603cba">
  <meta name="theme-color" content="#7e10d9">
  <meta name="description" content="We're experts in Ecommerce & Custom WordPress Websites" />
	<title>{title}</title>
</head>
<body>
	<slot />
  <footer>
    <nav><span>Big Commerce</span><span>Easy Digital Downloads</span><span>Dukaan</span></nav>
    <div class="footer-bar">
    © 2022 Slift. All rights reserved by Slift & Artusion.
    </div>
  </footer>
</body>
</html>

<style>
  :root {
    --font-size-base: clamp(1rem, 0.34vw + 0.91rem, 1.19rem);
		--font-size-lg: clamp(1.2rem, 0.7vw + 1.2rem, 1.5rem); 
    --font-size-xl: clamp(2.44rem, 2.38vw + 1.85rem, 3.75rem);

    --color-text: hsl(260, 11%, 15%);
    --color-bg: hsl(268, 50%, 96%);
		--color-border: hsl(268, 40%, 90%);
  }

  html {
    font-family: Avenir Next, 'Open Sans', 'Segoe UI', system-ui, sans-serif;
    font-size: var(--font-size-base);
    color: var(--color-text);
    background-color: var(--color-bg);
  }

  body {
    margin: 0;
  }

  :global(h1) {
    font-size: var(--font-size-xl);
  }

  :global(h2) {
    font-size: var(--font-size-lg);
  }

  :global(code) {
    font-family: Menlo, Monaco, Lucida Console, Liberation Mono, DejaVu Sans Mono,
			Bitstream Vera Sans Mono, Courier New, monospace;
  }

  footer nav, .footer-bar {
    text-align: center;
    position: fixed;
    left: 0;
    bottom: 0;
    width: 100%;
    background-color: #25222a;
    color: white;
    padding: 16px 0;
    font-size: 16px;
  }

  footer nav {
    bottom: 54px;
    background-color: #363041;
    padding: 24px 0;
  }

  footer nav span {
    color: white;
    text-decoration: none;
    margin-right: 40px;
    font-weight: 500;
  }
</style>
